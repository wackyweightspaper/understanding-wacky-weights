# @package _global_

init_dict:
  model_type_or_dir: distilbert/distilroberta-base
  model_type_or_dir_q: null
  freeze_d_model: 0
  agg: max
  fp16: true

config:
  tokenizer_type: distilbert/distilroberta-base
  matching_type: splade