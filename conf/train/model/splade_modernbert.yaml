# @package _global_

init_dict:
  model_type_or_dir: answerdotai/ModernBERT-base
  model_type_or_dir_q: null
  freeze_d_model: 0
  agg: max
  fp16: true

config:
  tokenizer_type: answerdotai/ModernBERT-base
  matching_type: splade